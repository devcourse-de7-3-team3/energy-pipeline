# --- Snowflake 관련 ---
apache-airflow-providers-snowflake==5.2.0        # Airflow 2.9.x 호환
snowflake-connector-python==3.10.1               # 안정 버전

# --- dbt (Snowflake 변환용) ---
dbt-core==1.8.3
dbt-snowflake==1.8.3

# --- ETL에 필요한 utility ---
pandas==2.2.2
requests==2.32.3
